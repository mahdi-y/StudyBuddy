apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-study-group
spec:
  selector:
    matchLabels:
      app: mysql-study-group
  template:
    metadata:
      labels:
        app: mysql-study-group
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          envFrom:
            - secretRef:
                name: study-group-secrets
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: study-group-mysql-pvc
