apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-task
spec:
  selector:
    matchLabels:
      app: mysql-task
  template:
    metadata:
      labels:
        app: mysql-task
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          envFrom:
            - secretRef:
                name: task-mysql-secrets
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: task-mysql-pvc
