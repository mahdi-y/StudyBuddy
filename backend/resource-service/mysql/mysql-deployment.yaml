apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-resource
spec:
  selector:
    matchLabels:
      app: mysql-resource
  template:
    metadata:
      labels:
        app: mysql-resource
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          envFrom:
            - secretRef:
                name: resource-mysql-secrets
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: resource-mysql-pvc
